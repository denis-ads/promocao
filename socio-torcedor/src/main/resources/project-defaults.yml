swarm:
  datasources:
    data-sources:
      SocioDS:
        driver-name: org.postgresql
        connection-url: jdbc:postgresql://localhost:5432/sociodb
        user-name: sociotorcedor
        password: socio@1
    jdbc-drivers:
      org.postgresql:
        driver-class-name: org.postgresql.Driver
        xa-datasource-name: org.postgresql.xa.PGXADataSource
        driver-module-name: org.postgresql