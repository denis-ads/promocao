swarm:
  datasources:
    data-sources:
      CampaignDS:
        driver-name: org.postgresql
        connection-url: jdbc:postgresql://localhost:5432/campanhadb
        user-name: campanha
        password: campanha@1
    jdbc-drivers:
      org.postgresql:
        driver-class-name: org.postgresql.Driver
        xa-datasource-name: org.postgresql.xa.PGXADataSource
        driver-module-name: org.postgresql